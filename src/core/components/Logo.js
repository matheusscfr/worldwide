import React from "react";
import { NavLink } from "react-router-dom";

const Logo = () => {
    return(
        <div className="logo">
        <NavLink className='logo_link'>
            <h1>WorldWide</h1>
        </NavLink>
        </div>
    )}

export default Logo